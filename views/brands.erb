<h4> Shoe Brands </h4>

<% if @brands.any?() %>
  <ul>
    <% @brands.each do |brand| %>
    <li><a href = '/brands/<%= brand.id() %>'><%= brand.name() %></a></li>
    <% end %>
  </ul>
    <% else %>
    <p>Sadly, we dont have any shoe brands yet. Add one Below! </p>
    <% end %>

  <form action = '/brands' method='post'>
    <label for='new_brand'>Enter a new Brand:</label>
    <input id='new_brand' name='new_brand' type='text'>
    <button type='submit'>Add a new brand</button>
  </form>
    <a href = '/'> Home </a>
    <a href = '/stores'> See all stores </a>
