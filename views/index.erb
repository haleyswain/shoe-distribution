<h2>Welcome to the Epicodus Shoe Distribution Center</h2>

<% if @stores.any?() %>
  <p>Click on a store to see which shoe brands they sell there:</p>

  <ul>
    <% @stores.each do |store| %>
      <li><a href="/stores/<%= store.id() %>"><%= store.name() %></a></li><br>
    <% end %>
  </ul>
<% else %>
  <p>There are no stores added yet!</p>
<% end %>

<h3>Here are all the shoe brands in this database:</h3>

<% if @brands.any?() %>
  <p>Click on a brand to see where it is sold:</p>

  <ul>
    <% @brands.each do |brand| %>
      <li><a href="/brands/<%= brand.id() %>"><%= brand.name() %></a></li>
    <% end %>
  </ul>
<% else %>
  <p>There are no brands added yet!</p>
<% end %>

<a href='/stores'>Click here to view a list of our stores</a>
<br>
<a href='/brands'>Click here to view a list of our shoe brands</a>
